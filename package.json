{
  "name": "caidoreflector-plus-plus",
  "version": "1.0.0",
  "description": "Advanced reflected parameter detector with scoring, encoded signal analysis, and contextual probes.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "build": "caido-dev build",
    "watch": "caido-dev watch"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@caido-community/dev": "^0.1.6",
    "@caido/plugin-manifest": "^0.3.0"
  }
}
